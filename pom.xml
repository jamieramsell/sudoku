<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.jamieramsell</groupId>
  <artifactId>sudoku</artifactId>
  <version>1.0.2</version>
  <packaging>jar</packaging>

  <name>Sudoku</name>
  <description>
    A variable-size Sudoku engine: composite grid model, backtracking solver,
    and symmetric puzzle generator. The model layer of an MVC stack.
  </description>
  <url>https://github.com/jamieramsell/sudoku</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://github.com/jamieramsell/sudoku/blob/main/LICENSE</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <name>Jamie Ramsell</name>
      <url>https://github.com/jamieramsell</url>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/jamieramsell/sudoku.git</connection>
    <developerConnection>scm:git:https://github.com/jamieramsell/sudoku.git</developerConnection>
    <url>https://github.com/jamieramsell/sudoku</url>
  </scm>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
    <checkstyle.version>10.17.0</checkstyle.version>
  </properties>

  <dependencies>
    <!-- JUnit 4 pulls in hamcrest-core 1.3 transitively. -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <!-- AllTests is a JUnit suite aggregating the *Test classes; let
               Surefire discover those classes directly to avoid running every
               test twice. -->
          <excludes>
            <exclude>**/AllTests.java</exclude>
          </excludes>
        </configuration>
      </plugin>

      <!-- Publish a sources jar alongside the main artifact for downstream reuse. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Enforce the project's snake_case Checkstyle rules during `verify`,
           matching the standalone CI lint job. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>3.4.0</version>
        <dependencies>
          <dependency>
            <groupId>com.puppycrawl.tools</groupId>
            <artifactId>checkstyle</artifactId>
            <version>${checkstyle.version}</version>
          </dependency>
        </dependencies>
        <configuration>
          <configLocation>checkstyle.xml</configLocation>
          <sourceDirectories>
            <sourceDirectory>${project.build.sourceDirectory}</sourceDirectory>
          </sourceDirectories>
          <includeTestSourceDirectory>false</includeTestSourceDirectory>
          <failOnViolation>true</failOnViolation>
          <violationSeverity>error</violationSeverity>
        </configuration>
        <executions>
          <execution>
            <id>checkstyle-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/jamieramsell/sudoku</url>
    </repository>
  </distributionManagement>

</project>
